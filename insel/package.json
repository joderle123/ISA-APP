{
  "name": "lumo-insel",
  "private": true,
  "version": "0.1.0",
  "description": "LUMO – Open-World-Insel für die Annexe Junglinster (CDSE). Build: npm install && node build.mjs",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node tests/core.mjs && node tests/smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "three": "0.186.1"
  }
}
